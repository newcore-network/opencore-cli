import { Client } from '@open-core/framework';

// Bootstrap the client
Client.bootstrap({
  features: [
    // Add your client features here
  ],
});

console.log('{{.ProjectName}} client started!');

