{
  "name": "{{.ProjectName}}",
  "version": "1.0.0",
  "private": true,
  "description": "OpenCore server project",
  "scripts": {
    "dev": "opencore dev",
    "build": "opencore build"
  },
  "devDependencies": {
    "@open-core/cli": "latest",
    "typescript": "^5.3.3",
    "esbuild": "^0.27.2",
    "esbuild-plugin-swc": "^1.0.1",
    "@swc/core": "^1.15.7"
  },
  "dependencies": {
    "@open-core/framework": "latest"{{if .InstallIdentity}},
    "@open-core/identity": "latest"{{end}}
  }
}