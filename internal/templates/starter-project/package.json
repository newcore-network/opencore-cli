{
  "name": "{{.ProjectName}}",
  "version": "1.0.0",
  "private": true,
  "description": "OpenCore server project",
  "scripts": {
    "dev": "opencore dev",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "@open-core/cli": "latest",
    "typescript": "^5.3.3",
    "esbuild": "^0.24.0",
    "esbuild-plugin-swc": "^1.0.0",
    "@swc/core": "^1.3.0"
  },
  "dependencies": {
    "@open-core/framework": "latest"{{if .InstallIdentity}},
    "@open-core/identity": "latest"{{end}}
  }
}

