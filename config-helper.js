/**
 * Helper function to define OpenCore configuration with TypeScript support
 * This is a simple identity function that provides type inference
 * @param {object} config - OpenCore configuration object
 * @returns {object} The same configuration object
 */
function defineConfig(config) {
  return config;
}

module.exports = { defineConfig };

